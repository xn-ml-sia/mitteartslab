# Retired pages

These pages are no longer served in production. They are kept for reference and can be restored from git history.

| Page | Files |
|------|-------|
| Services | `services.html`, `public/services.*` |
| Stoneface / landing | `stoneface.html`, `public/stoneface.*` |

Shared site assets (`/public/styles.css`, vendor GSAP, etc.) remain in the repo root `public/` folder.
