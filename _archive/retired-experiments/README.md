# Retired archive experiments

Heavy archive sections removed from the live `/archive` page (canvas portraits, particle vessel, shader decks).

Restore by copying `archive-heavy-sections.html` back into `archive.template.html` and re-adding `<script type="module" src="./public/app.js">` plus required GSAP plugins.

Requires `public/app.js`, `shader-deck-shaders.js`, `interlude-*.js` when fully wired.
